#!/usr/bin/env ruby

require 'bundler/setup'
require 'pry'

require_relative '../lib/nicorepo/cli'

Nicorepo::Cli::Runner.run
# nicorepo = Nicorepo.new
# report = nicorepo.videos(5)
# binding.pry


# nico = Nicorepo.new
# nico.videos(10, max_page: 100)
# nico.videos(10, since: '2017-05-01', order: :asc)
# nico.fetch(:videos, 10, max_id: 'xxxxxxxxxxxxxxxxxxxxxx')
# nico.fetch(/user\.video/, 10)
#
# report = nico.videos(10, max_page: 100)
# report.json
# => [{...}, {...}, ...]
# report.format(Nicorepo::Formatter::Videos)
# Nicorepo::Formatter::Videos.apply(report.json)
# => [{
#   body: '〜さんが動画を投稿しました。',
#   info: '長いゆで卵を作ってみた'
# }]
#
